<%= simple_form_for(@weight_measurement, remote: true) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :value %>
    <%= f.input :unit %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "save" %>
  </div>
<% end %>
